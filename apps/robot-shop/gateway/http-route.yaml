apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: web-http-route
  namespace: robot-shop
spec:
  parentRefs:
    - name: robot-shop-gateway
  hostnames:
    - "robot-shop.example.com"
  rules:
    - backendRefs:
        - name: web
          port: 8080